# TALARIA Phase 1: Self-Supervised Pre-training

# Model Architecture
embed_dim:        320
token_patch_size: 16
depth:            12
d_state:          16
expand:           2
drop:             0.1

# Reconstruction Decoder
decoder_dim: 128

# Masking
mask_ratio: 0.50

# Dataset Paths
lits_root:  null
tcia_root:  /home/introai14/talaria/talaria_v5/nii_subset

# Multi-scale patch ensemble
patch_sizes: [96, 128]
strides_ms:  [48, 64]

# Training
epochs:      100
batch_size:  2
num_workers: 0

# Optimizer
lr:           1.0e-4
weight_decay: 0.05

# Checkpoint
save_every: 2
data_dir:  /home/introai14/projects/talaria-main/datasets/Task03_Liver
json_path: /home/introai14/projects/talaria-main/datasets/Task03_Liver/dataset.json
num_workers: 4
w_mae: 1.0
w_rot: 0.2
